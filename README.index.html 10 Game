# index.html-10-Game-
index.html 10 Game 
<!DOCTYPE html>
<html lang="bn">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Tap Game Demo</title>
<style>
  body { font-family: Arial, sans-serif; background:#f4f7fb; text-align:center; padding:20px; }
  h2 { margin-bottom:10px; }
  #balance { font-size:22px; font-weight:bold; margin:15px 0; }
  .game { display:flex; align-items:center; justify-content:left; margin:12px auto; width:280px; padding:10px; border-radius:10px; background:#fff; box-shadow:0 2px 6px rgba(0,0,0,0.1); cursor:pointer; }
  .game img { width:40px; height:40px; margin-right:12px; }
  .locked { opacity:0.5; cursor:not-allowed; }
  input, button { padding:10px; margin:5px; border-radius:8px; border:1px solid #ccc; }
  button { background:#4CAF50; color:#fff; border:none; cursor:pointer; }
</style>
</head>
<body>

<h2>🎮 Tap Game Demo</h2>
<div id="balance">💸 0 💸</div>

<div id="keyDiv">
  <p>🔑 Key বসান গেম আনলক করতে</p>
  <input type="text" id="keyInput" placeholder="Enter Key">
  <button onclick="checkKey()">Unlock</button>
</div>

<div id="games" style="display:none;">
  <div class="game" id="game1"><img src="color.png" alt="Color"> <span>1) Color Ball 🎨</span></div>
  <div class="game locked" id="game2"><img src="number.png" alt="Number"> <span>2) Number Quiz 🔢</span></div>
  <div class="game locked" id="game3"><img src="emoji.png" alt="Emoji"> <span>3) Emoji Match 😀</span></div>
</div>

<script>
const correctKeys = ["BDBOY123","JOINBD5054","SUBYT2025"]; // এখানে টেলিগ্রাম থেকে আসা KEY গুলো বসান

let balance = 0;
let unlockedGame = 1;

function updateBalance(amount){
  balance += amount;
  document.getElementById('balance').innerText = `💸 ${balance} 💸`;
}

function checkKey(){
  let key = document.getElementById('keyInput').value.trim();
  if(correctKeys.includes(key)){
    alert("✅ Key ঠিক আছে! গেম আনলক হয়েছে।");
    document.getElementById('keyDiv').style.display = 'none';
    document.getElementById('games').style.display = 'block';
    refreshGames();
  } else {
    alert("❌ ভুল Key! আবার চেষ্টা করুন।");
  }
}

function refreshGames(){
  for(let i=1;i<=3;i++){
    let g = document.getElementById('game'+i);
    if(i<=unlockedGame) g.classList.remove('locked');
    else g.classList.add('locked');
  }
}

for(let i=1;i<=3;i++){
  document.getElementById('game'+i).addEventListener('click', function(){
    if(i>unlockedGame){ alert("আগের গেম শেষ করুন!"); return; }
    alert("Game Completed! +10 coins");
    updateBalance(10);
    if(unlockedGame < i+1) unlockedGame = i+1;
    refreshGames();
  });
}
</script>

</body>
</html>
